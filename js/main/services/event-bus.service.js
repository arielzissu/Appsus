
export const eventBus = new Vue()

// export const EMAIL_SENT_MSG  = 'showMsg';

// eventBu  s.$on(EVENT_PUK, (num)=>{

//     const msg = {
//         txt: 'Got a Puk!',
//         type: 'danger'
//     }

    // eventBus.$emit(EMAIL_SENT_MSG , msg)
// })
